# Validation Result Records

Store one immutable or revision-controlled result record per executed test or controlled test session.

Recommended naming:

```text
EXE-YYYYMMDD-NNN_<test-case-id>.md
```

Do not overwrite an earlier result to change its outcome. Add a new execution record and link supersession or rerun relationships.
